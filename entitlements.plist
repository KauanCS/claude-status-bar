<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Required under the hardened runtime to send Apple Events (NSAppleScript /
       AppleEvent-based automation). Without this, TerminalFocusTracker's
       osascript-equivalent calls to Terminal.app fail outright on a
       Developer-ID-signed, hardened-runtime build, even with
       NSAppleEventsUsageDescription set in Info.plist. -->
  <key>com.apple.security.automation.apple-events</key>
  <true/>
</dict>
</plist>
